# 현재 합의와 다음 작업

## 2026-09-15 발주서 대행견적 이력불러오기 + 원가 자동기재 (커밋 4342a08)

- 요청: 인포라드 등으로 나간 대행견적서 품목을 에드워드(IV/SV/AK)로 발주할 때 원가를 매번 직접 입력하지 않도록.
- `app/admin/purchase-orders/page.tsx`에 이력불러오기 버튼 추가(`/api/admin/proxy-quotes/history` 재사용), 선택 시 품목·수량은 유지하고 단가만 판매가 대신 `Product.costPrice`로 자동 대체. 불러온 후 금액 수정·삭제는 자유.
- 실제 데이터(인포라드 견적 id=230)로 검증하다가 파트번호가 사후에 정정된 사례(`D14641000`→`D14642000`) 발견 → 문자열 매칭 대신 `QuoteItem.productId` 우선 매칭으로 설계 변경, `/api/admin/proxy-quotes/history` 응답에 `productId` 추가(기존 대행견적서 화면엔 영향 없음).
- tsc/eslint/next build 통과, 라이브 API로 재현 검증(14개 품목 전원 매칭 성공) 후 push, Vercel Production 배포(Ready) 확인 완료.

## 2026-09-15 블로그 발행 → X 초안 자동생성 연결 (커밋 1a3411a)

- 대표님 요청: "새로 생성되는 블로그에 맞춰 X 글도 초안 작성해서 게시하면 좋겠다" → 방향 제안(발행 시 초안 자동생성, 게시는 계속 수동 승인) 승인받고 연결.
- `scripts/generate-x-posts-from-blog.mjs`에 `--id` 옵션 추가(블로그 글 1건 지정 생성, dedup 유지). `블로그/approve_post.py`가 `upload_post.py` 출력에서 발행된 블로그 id를 파싱해 업로드 성공 직후 자동 호출하도록 연결.
- 게시(실제 X 업로드)는 자동화하지 않음 — 생성된 초안은 PENDING으로 쌓이고 `/admin/x-posts`에서 관리자가 직접 승인·게시.
- 검증: subprocess 호출 시뮬레이션(blog id=76)으로 정상 생성 확인, dedup·not-found 케이스도 확인. push·원격일치 확인 완료.

## 2026-09-15 X 게시 글자수 버그 코드 수정 + 잔여 초안 5건 축약 (커밋 cb8b672)

- Astra의 재조사·시험 게시 성공(아래 항목)을 이어받아 클로드에서 근본 수정 진행. `lib/x-text-length.ts` 신규 작성 — X 공식 weightedLength(한글·CJK·전각문자 2자, 그 외 1자) 계산 함수.
- 3곳 교체: 초안 생성 검사(`generate/route.ts`), 게시 전 사전검사(`app/api/admin/x-posts/route.ts` PATCH publish — 초과 시 400으로 막아 헛된 403 재현 방지), 관리자 화면 글자수 표시(`x-posts/page.tsx`, 초과 시 빨간색).
- DB의 PENDING/APPROVED 잔여 5건(id 1~5) 전수조사 → 전부 X 기준 초과 상태였음 확인(예: id=3은 화면 226자, 실제 388자). 핵심 내용 유지하며 5건 모두 축약해 content만 DB 직접 수정, 전부 280자 이내 재확인.
- `tsc --noEmit` 통과 → 커밋(attribution 누락으로 1회 amend) → push → `git rev-parse HEAD`/`git ls-remote` 원격 일치 확인 완료.
- **다음 확인할 것**: 대표님이 이 5건을 관리자 화면에서 "게시" 버튼으로 재시도해 정상 게시되는지 확인 필요. 프로젝트는 더 이상 보류 상태 아님.

## 2026-09-15 X 게시 재개 시험 — 기존 글 축약 후 게시 성공

- 대표님 승인으로 XPost id=6 정기점검 글만 축약 저장했다. X 기준 409→226자(현재 화면은 일반 길이 139자로 표시).
- 원문 백업: `tools/x-post-6-before-shortening.json`(로컬 전용). 홈페이지 코드·다른 글·X 키 변경 없음.
- 작업 환경에서 홈페이지 관리자 인증을 확인하지 못해 대표님이 직접 게시했다. 성공 보고 후 DB 재조회로 POSTED·tweetId `2099639323054448660`·한국 9/15 08:19:58 게시를 확인했다. https://x.com/smartechvacuum/status/2099639323054448660
- 기존 Claude의 "X 자체 버그 확정" 결론을 반복하지 않는다. 같은 앱·키·코드로 축약 글 게시 성공. 한글 가중 길이 검사 누락은 확정 결함이며 이번 성공은 길이 문제 가설을 강하게 지지한다. 과거 모든 403의 원인을 단정하지 않는다.
- 재발 방지용 글자 수 검사·초안 생성 코드 수정과 다른 글의 축약·게시는 아직 미착수. 이번 승인은 글 한 건 시험에 한정. 세부 기록은 `docs/astra_work_log.md` 최신 항목.

## 2026-09-14 제품 단가 버그 근본 수정 — 카테고리별 세부검색 DB 실시간화 (커밋 3f83979)

- 계기: 대표님이 EMF20/EMF3 단가가 높게 세팅돼 있어 DB에서 직접 수정, 확인 과정에서 2026-06-18에 미해결로 남았던 "카테고리 선택 후 세부검색은 여전히 `lib/productCatalog.json` 고정가 사용" 버그를 재발견.
- 대표님 결정: JSON 값을 수동 삭제하지 말고 코드로 근본 해결. `app/api/products`에 `partNos` 다건조회 파라미터 추가 + `PumpSelector.tsx` 세부검색 목록이 이제 DB 실시간 가격을 표시하도록 수정.
- 이 컴포넌트는 EM(미스트필터)뿐 아니라 JSON에 정의된 전체 카테고리가 공유해서, 다른 카테고리의 동일 잠재 버그도 함께 해소됨.
- `tsc`·`eslint`(신규 코드 기준)·`next build` 통과, 로컬·라이브 API 응답으로 실제 DB 가격 반영 확인 완료. push·배포·라이브 검증 모두 완료.

## 2026-09-14 블로그 월요일 발행 — 0914-반도체-전해연마-배관 (id=97)

- `0914-반도체-전해연마-배관` 발행 완료: https://www.smartechvacuum.com/blog/97 (`approve_post.py`, upload-queue.json day1 approved/uploaded=true).
- 발행 전 사진 재사용 사고 2건 발견·수정: ①사진1이 썸네일과 완전 동일 파일 ②사진3이 미발행 `0911-아웃가스-원리`의 사진3과 동일 파일(`hardware.png`). 둘 다 새 도식으로 교체 후 재검수·재승인.
- **다음 발행(day2 `0917-진공-물-끓음`, 목요일) 전에는 습관적으로 `images/` 폴더 내 전체 PNG를 MD5로 상호 대조**하고, 다른 미발행 글 폴더와도 대조할 것. image-maker.md 6단계 절차 준수만으로는 이런 중복이 막히지 않았음이 이번에 확인됨.
- `0911-아웃가스-원리`는 여전히 미발행 상태(별도 사용자 요청 대기). 이번 사진3 교체는 0914 쪽만 바꿨고 0911 폴더는 손대지 않음.

## 2026-09-13 Search Console 구조화 데이터 경고 — availability 필드 추가·수정

- Search Console이 판매자 목록/제품 스니펫 구조화 데이터에서 `offers.availability` 누락을 경고(심각하지 않은 문제).
- 1차: `app/products/[partNo]/page.tsx`의 productSchema에 `"availability": "https://schema.org/InStock"` 고정값 추가(대표님 선택, 검토 대안은 `PreOrder`). 커밋 `997d757`.
- 2차(코드리뷰에서 발견·수정): InStock 고정이면 품절(`stock<=0`)·단종(`isDiscontinued`) 제품도 "재고있음"으로 잘못 노출됨. `product.isDiscontinued`→`Discontinued`, `stock<=0`→`OutOfStock`, 그 외 `InStock`으로 분기하도록 수정. 커밋 `72dd1cc`.
- 화면(UI)에는 재고 문구 자체가 없어 사용자 화면 영향 없음. 구글 크롤링 반영은 며칠~몇 주 소요.
- push 완료, `reviewed` 포인터도 `72dd1cc`로 전진 완료.

## 2026-09-11 최신 상태 — 블로그 자동 업로드

- `블로그/auto_upload.py`의 `uploaded` 상태 버그를 수정했다. `True`는 이미 업로드된 것으로 건너뛰고, `False` 또는 `None`은 미업로드로 처리하며, 문자열 등 이상값은 중단한다.
- Python 문법 체크, 자동 업로드 안전성 테스트 7개, PowerShell 스케줄러 계약 테스트를 모두 통과했다.
- 관련 파일 4개만 `a92a643 fix: 블로그 자동 업로드 예약 및 상태 검증`으로 로컬 커밋했다. 기존 미커밋 문서·콘텐츠 변경은 보존했다.
- Windows 예약 작업은 당분간 적용하지 않는다. 요청이 있는 월요일에는 `python .\\블로그\\auto_upload.py --slot day1`, 목요일에는 `python .\\블로그\\auto_upload.py --slot day2`로 수동 실행한다.
- 자동화가 필요해지면 `setup_scheduler.ps1 -WhatIf`를 먼저 확인한 뒤 관리자 권한으로 적용한다.
- 현재 `master`는 `origin/master`보다 1커밋 앞서 있으며, 메모리 기록 후 push할 예정이다.

기준: 2026-09-10 홈페이지 개선 진행 승인 및 9/9 운영 합의. 과거 메모리의 상충하는 일정·도구 설명보다 이 합의가 우선한다.

## 세션 종료·다음에 이어갈 지점 — 2026-09-10

- 대표님이 오늘 작업을 마치고 모두 메모리에 저장한 뒤 다음에 이어가자고 요청했다. 이번에는 기록 저장만 마무리하며 후속 구현·글 발행을 시작하지 않는다.
- 홈페이지 개선 코드 `a5ae84c`, 검증 기록 `b5dc8a8` 모두 GitHub master에 push 완료. 두 커밋 모두 Vercel success 확인. 마지막 원격 조회도 `b5dc8a8efa5a257eeab143c0ae39490177d3a1a6`과 일치했다.
- 오늘 완료: 홈페이지 첫 표시·이미지 전달, 제품 목록 초기 제목, 잘못된 재고/공통 FAQ 검색 정보, 블로그 링크·중복 제목·요약·대표 이미지 개선. 빌드 및 공개 주요 5개 페이지·제품 검색 검증 완료.
- 다음 홈페이지 작업 우선순위: 기존 기술 글의 확인된 오류부터 1~2편 교정 → 남은 모바일 반응 지연 분석 → 공개 사진/카탈로그 접근 경로 점검. Search Console·GA4·AI 인용 실적은 계정 자료 확인 후 판단한다.
- 금요일 `0911-아웃가스-원리`는 미발행·별도 요청 대기. 다음주 9/14·9/17은 후보 5개 중 2개 선택 대기. Windows 월·목 예약 실제 적용은 관리자 권한 문제로 미완료다.
- 어제부터 남은 블로그 원고·자동화·운영 규칙 변경과 원시 진단 파일은 로컬에 남아 있다. 홈페이지 커밋과 함께 전부 push됐다고 가정하지 말고 다음 작업 시 변경 범위를 다시 확인한다.
- 상세 이력은 `docs/astra_work_log.md`, 홈페이지 진단과 개선 증거는 `docs/audits/2026-09-09-seo-geo-review.md`, `docs/audits/2026-09-10-seo-geo-improvements.md`, `docs/audits/2026-09-10-seo-verification.json`에 있다.

## 9/10 홈페이지 개선 진행

- 홈페이지 공통 SEO·GEO 오류 개선을 진행했다. 설명문 표시 지연·제품 카드 이미지·제품 목록 초기 제목·재고/FAQ 검색 정보·블로그 링크/중복 제목/설명/대표 이미지가 대상이다.
- 최종 빌드·로컬/공개 페이지·제품 검색 검증 통과. 코드 `a5ae84c` master push·원격 일치·Vercel success·공개 반영 확인 완료.
- 모바일 Lighthouse 동일 주소 1회씩 측정: 성능 55→80, LCP 7.80→2.97초, 전송량 약 27% 감소. 검색 순위·실사용자 성과 점수는 아니다.
- [상세 결과와 남은 진단 항목](../audits/2026-09-10-seo-geo-improvements.md).
- 기존 기술 글 DB 교정, 계정별 검색 실적 확인, Windows 예약 적용, 금요일 글 발행은 별도 상태를 유지한다.

## 작업 방식

- 대표님은 Codex/Astra high로 전환했고 앞으로 여기서 대부분 작업한다. 확인되지 않은 UI 모델 상태를 변경했다고 주장하지 않는다.
- 꼼꼼한 검토·운영 문서 개선·사이트 SEO/GEO 점검·월목 전환은 승인됐다. 30~60분 단위로 검증하면서 승인 범위 안에서 이어간다.
- 루트 AGENTS 포함 보호 파일 11개, 디자인 토큰 8개, 기존 패키지 제한은 유지한다. 인수인계 문서의 전체 목록을 읽는다.
- 승인된 변경은 범위를 명시해 커밋·push 전 검토·원격 확인까지 진행한다. 다른 사람의 미커밋 수정·로컬 설정은 포함하지 않는다.
- 대표님께는 한 단계씩 설명하고 “간단히/쉽게”라는 표현을 쓰지 않는다. 질문/의견만 요청하면 코드 작업을 시작하지 않는다.

## 블로그

- **2026-09-14(월) 발행 완료**: `0914-반도체-전해연마-배관` id=97. day2 `0917-진공-물-끓음`(목)은 원고·검수 완료, 승인·발행 대기.
- **2026-09-14 월요일부터 월·목 주 2편**, 작성은 Astra. 다음주 9/14·9/17 후보 5개 중 대표님이 2개를 선택한다.
- 후보에 일반 진공이론·안전 주제 1개 포함. tracker·매트릭스·미발행 원고·공개 글을 함께 비교한다.
- 후보가 선택되기 전 다음주 글을 쓰거나 큐를 채우지 않는다. 후보를 고르는 것과 사진·원고·예약 승인은 별개다.
- **`0911-아웃가스-원리`**: 미발행. 원고 교정과 검사 완료. 금요일에 대표님이 “블로그글 업로드해줘”라고 말하면 최종 원고·사진 확인 후 그 글만 발행한다. 임의 조기 발행·예약 승인 없음.
- 9/9 조사: 로컬 final 52개(기술 50·납품 1·보도 1), tracker는 누락 원고 4개 보완으로 **46→50개**, 공개 사이트맵 블로그 54개 URL 모두 HTTP 200. 서로 다른 집합이므로 숫자를 서로 대체하지 않는다. 기존 원고 보완으로 로테이션이나 상담기록 사용 이력은 바꾸지 않았다.
- `upload-queue.json`의 기존 W37 발행 기록 보존. W38 원고·승인 데이터는 아직 없다.
- `approve_post.py`는 즉시 업로드 도구다. 미래 예약 승인용으로 실행하지 않는다.

## 예약 작업 상태 — 실제 적용 미완료

- 월·목 전환 코드 및 테스트 준비. ISO 주차·날짜·승인·중복 검사를 보강했다.
- Windows `-WhatIf` 검토 성공. 기존 월·수·금 작업 3개 XML 백업 확보.
- 실제 실행은 첫 `Register-ScheduledTask`에서 `Access is denied (0x80070005)`로 중단. 도구 샌드박스 밖에서도 Windows 관리자 권한이 부족했다.
- 재조회 결과 기존 월·수·금 작업 3개가 그대로 유지됐다. 목요일 새 작업은 등록되지 않았다.
- 새 Python 날짜 검사는 다음주 수요일 실행을 건너뛰지만, 목요일 작업이 없으면 목요일 자동 발행도 없다. “예약 전환 완료”라고 보고하면 안 된다.
- 다음 행동: Windows 관리자 PowerShell에서 아래 스크립트 1회 실행 후 작업 이름·시작일·비활성화 상태를 조회한다. 실행해도 글 승인 상태는 바뀌지 않는다.

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File 'C:/Users/rokmc/smartech/블로그/setup_scheduler.ps1'
```

## SEO/GEO 후속 우선순위

- [사이트 종합 점검](../audits/2026-09-09-seo-geo-review.md), [기존 글 검토와 후보](../audits/2026-09-09-blog-review.md).
- 기존 글 전체를 조금씩 고치기보다 기술 오류·검색 의도·근거가 부족한 글부터 교정한다. 표면 수정이나 발행 날짜만 갱신하지 않는다.
- 공개 자료 점검은 색인 여부·실제 유입·문의 증가·AI 인용 성과 검증이 아니다. Search Console·GA4 최신 데이터는 별도 확인 대상이다.
- 현재 작업의 커밋·배포·검증 최종 상태는 `docs/astra_work_log.md` 최신 기록을 확인한다.
