# Astra 작업 기록

## 2026-09-09 — 0911-아웃가스-원리

- 사용자 승인 주제: “진공 챔버에서 계속 나오는 가스는 어디서 올까? — 아웃가스의 원리”
- 공식 기술 자료 8개를 바탕으로 리서치 완료. 아웃가스(탈착·확산·기화), 실제 누설, 가상 누설, 투과를 구분하고 일률적인 가열 조건은 제시하지 않음.
- 산출물 폴더: `블로그/output/기술블로그/2026-09/0911-아웃가스-원리/`
- 작성 파일: `final.md`, `google.md`, `naver.md`, `meta.txt`, `faq.json`, `marketing.txt`
- 이미지 5개 제작·시각 확인 완료. 현장사진 대신 설명 도식 4개와 부품 참고 이미지 1개를 사용했으며, `no-field-photo.md`에 사유 기록.
- 품질 검사: `python 블로그/check_quality.py "기술블로그/2026-09/0911-아웃가스-원리"` 전체 통과. final.md 2,010자, naver.md 2,067자, 이미지 마커 4개와 사진 파일 4개 일치.
- 업로드·사진 승인·금요일 예약 등록은 아직 하지 않음. 사용자가 사진과 내용을 확인한 뒤 별도로 진행.

## 2026-09-09 — 업로드 진행 기준 합의

- 사용자가 금요일에 “블로그글 업로드해줘”라고 말하면 `0911-아웃가스-원리` 글을 홈페이지 블로그에 업로드한다.
- 업로드 전 사진과 최종 원고를 확인하고, 별도 사진 승인이 필요한 경우 그 절차를 먼저 진행한다.

## 2026-09-09 — Astra 운영 이관·SEO/GEO 점검·월목 전환

### 승인 범위

대표님이 외출 중에도 꼼꼼히 이어서 작업하도록 승인. Codex 중심 운영으로 전환하고 규칙·메모리·커밋/push 절차를 검토하며 홈페이지 전반 SEO/GEO 점검과 다음주 후보 5개를 준비한다. 다음주 두 글은 선택을 기다리고 금요일 글은 그날 업로드 요청을 기다린다.

### 완료한 변경

- Claude 메모리 Markdown 97개를 비공개 로컬 스냅샷으로 보존하고 파일별 SHA-256 일치 확인. 원본 수정 없음. `docs/memory/README.md`, `current-state.md`, manifest를 새 세션 시작점으로 연결했다.
- root CLAUDE·handoff·README 및 블로그 규칙/에이전트/가이드 9개를 대조·정리. 보호 파일 11개·색상 8개·패키지 규칙 유지. 실제 파일 접근과 자동 기억/훅 실행을 구분하고 커밋→검토→push→배포 확인·기록 책임을 통일했다.
- README의 사용하지 않는 Geist 설명을 제거하고 과거 디버깅/디자인 제안에는 역사 기록임을 표시했다.
- 출처 표시 금지, 허구 경력·사례 유도, 기술 조건을 제거하는 단정, 근거 없는 SEO 수치·FAQ 혜택을 정리했다. 최신 공식 Google/네이버 자료와 내부 편집 규칙을 구분했다.
- 월·목 전환 Python/PowerShell 코드 및 검증 준비. 날짜·주차·승인·발행 상태를 점검하며 미래·과거 큐의 오발행을 방지한다. 기존 큐와 승인 값은 변경하지 않았다.
- tracker 누락 원고 4개를 작성완료로 보완(46→50). 공개 미확인은 unverified, 금요일 원고는 draft. 매트릭스 누락 12칸도 기존 원고와 대조해 보완했다. 순환 인덱스 10과 상담기록은 유지했다. 다음주 후보는 완료/예약 처리하지 않았다.
- `0911-아웃가스-원리`: 반복 문장 교정, 공식 출처 8개, final/google 동일, final 실질 2,294자·네이버 실질 2,179자·메타 150자·FAQ 6개. 기존 이미지 5개 유지. 기존 2,010자 검사 통과 기록보다 이번 교정본이 최신이다.
- 블로그 공통 표시: Markdown 공식 자료/내부 링크를 클릭 가능하게 처리하고 React 텍스트로 HTML 삽입을 방지했다. 중복 제목·내부 편집 주석을 제거하고 JSON-LD의 `<`를 이스케이프했다. 기존 DB 글 본문·URL은 수정하지 않았다.
- `seo-check`의 “전체 SEO 정상” 표현을 선언 검사로 한정하고 client 파일의 metadata 혼용도 실패로 처리했다.
- `.gitignore`에 하위 Claude 로컬 설정·Windows 작업 XML 백업·비공개 메모리 스냅샷을 제외했다. 기존 로컬 설정 파일 자체는 수정하지 않았다.

### 홈페이지 점검 결과

- 공개 사이트맵 1,162개, 중복 0개. 공개 블로그 54개 포함 표본 102개 모두 HTTP 200. HTTP 검사와 검색 색인 확인은 별개다.
- 최초 공개 표본에서 블로그 중복 H1 52개, 블로그 본문 링크 0/54, description 누락 1개, 제품 목록 초기 HTML H1 부재, 제품 InStock 고정 및 공통 FAQ 불일치 위험 확인.
- Lighthouse 13.4.1: 홈 모바일 성능55/접근성97/기술준수100/SEO100, LCP11.2초. 홈PC84/97/100/100, LCP2.8초. 블로그96 모바일84/96/100/100, LCP4.1초. 각1회 실험실 측정이며 실제 사용자·순위·GEO 점수가 아니다.
- [사이트 점검 보고서](audits/2026-09-09-seo-geo-review.md), [기존 글·후보 보고서](audits/2026-09-09-blog-review.md), 측정 HTML/JSON을 저장했다.
- 기존 글의 기술 오류·내부 모순·근거 누락을 우선 교정하도록 10편 공개 URL과 남아 있는 문제 문장을 대조했다. 아직 해당 10편 DB 원고를 일괄 수정하지 않았다.

### 검증

- `python -B -m unittest discover -s 블로그 -p test_auto_upload_schedule.py -v`: 6개 그룹/25개 시나리오 PASS.
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./블로그/test_setup_scheduler.ps1`: 모의 예약/백업/비활성화 검사 PASS, 실제 예약과 DB 변경 없음.
- `node node_modules/tsx/dist/cli.mjs scripts/test-blog-inline.tsx`: 링크·강조·HTML 이스케이프·위험 URL·중복 제목/주석 검사 PASS.
- `node node_modules/typescript/bin/tsc --noEmit --incremental false`: PASS.
- 수정 TSX 2개 ESLint: PASS. 기존 페이지의 등록되지 않은 Next lint rule 비활성화 주석 3개를 제거했다. 전 저장소 lint 통과를 주장하지 않는다.
- `npm.cmd run seo-check`: 13개 페이지 PASS.
- `npm.cmd run build`: Prisma DLL 교체가 EPERM으로 실패. 샌드박스 밖 재시도도 같았다. 기존 실행 프로세스는 중단하지 않았다.
- 스키마를 바꾸지 않았으므로 기존 Prisma client로 `node node_modules/next/dist/bin/next build` 실행: 컴파일·타입·130개 정적 페이지 생성 PASS.
- 임시 로컬 production 서버의 `/blog/96` HTTP200: H1 2→1, title/canonical 동일, 이미지3→3, JSON-LD 구문오류0. 서버의 처음 DB 접근은 샌드박스 네트워크에서 실패해 권한 있는 읽기 전용 검증으로 재실행했다. [검증 JSON](audits/2026-09-09-local-blog-verification.json).
- tracker 작성 원고50개, 폴더 중복0, 기술 원고 누락0 확인. 원고 출판 여부는 별도다.

### Windows 예약 적용 미완료

- 실제 `-WhatIf` 계획 확인 성공. 기존 월·수·금 작업 XML3개 백업 완료.
- 실제 스크립트는 첫 Register-ScheduledTask에서 OS `Access is denied (0x80070005)`로 중단. 자동 승인 검토 거절이 아니라 Windows 관리자 권한 부족이다.
- 기존3개 작업이 그대로이고 목요일 새 작업이 없음을 재조회했다. 새 코드만으로는 목요일 자동 실행이 생기지 않는다.
- 대표님 복귀 후 관리자 PowerShell에서 준비된 스크립트 실행·작업 조회가 남는다. 큐를 미리 채우거나 글을 승인하지 않았다.

### 남겨둔 상태

- 금요일 글과 다음주 2편의 사용자 선택/원고·사진·발행 승인은 대기.
- 검색 실적·AI 인용·문의 전환의 최신 계정 데이터는 미확인. 다음 개선은 기술 오류가 있는 기존 글과 모바일 첫 화면 속도가 우선이다.
- 작업 시작 시 master는 원격과 동일했고 reviewed 백로그는10개였다. 이번 변경 검토만으로 과거10개 전체를 검토한 것은 아니므로 reviewed를 임의 전진시키지 않는다.
- `블로그/quality-log.jsonl`과 `upload-log.txt`에는 시작 전/자동 실행 기록이 섞여 있어 파일 전체를 이번 커밋에 포함하지 않는다. 이번 검사 결과는 원고별 검증 메모와 이 로그에 별도로 기록했다.
- 커밋·push·실제 배포 결과는 아래 후속 항목에 기록한다.

## 2026-09-10 — 홈페이지 SEO·GEO 공통 오류 개선

- 대표님이 9/9 홈페이지 진단에 따른 개선 진행을 승인했다. 홈페이지 설명문 표시 지연·제품 카드 이미지 전달·제품 목록 초기 제목·고정 재고/FAQ 검색 정보·블로그 설명/대표 이미지를 개선했다.
- 9/9 미반영 블로그 공통 링크·중복 제목 처리와 SEO 검사 문구도 검토해 이번 홈페이지 코드에 포함한다. 기존 블로그 원고·자동 발행·Windows 예약 변경은 포함하지 않는다.
- 변경 전 파일을 `tools/seo-backup-2026-09-10/`에 백업했다. 보호 파일 11개 변경 없음.
- 최종 `npm.cmd run build` 성공(Prisma 생성·타입 검사·정적 페이지 130개). 별도 타입 검사·13개 SEO 선언 검사·블로그 링크/설명/이미지 검사 통과. 변경 TS/TSX ESLint 오류 0, 기존 미사용 import 경고 1.
- 최종 로컬 production 브라우저: 주요 5개 페이지 HTTP 200, H1 각 1개, 제품 목록 초기 HTML H1 존재, 요약 설명 누락 보완, 재고 고정/공통 FAQ 제거, RV12 제품 검색 정상, 브라우저 오류 0. 모바일 기존 간격도 확인했다.
- 제한 환경 DB 연결 실패는 권한 있는 로컬 서버로 해결해 재검증했다. 최초 실패 응답을 성공 검사에 포함하지 않는다.
- [변경·검증·남은 항목](audits/2026-09-10-seo-geo-improvements.md). 원격 커밋·배포·공개 성능 재측정 결과는 후속 기록한다.
