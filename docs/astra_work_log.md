# Astra 작업 기록

## 2026-09-24 — (클로드) 회사소개서 HTML 1차본 (코드 변경 없음)

- 대표님 요청: 옛 `바탕화면/회사소개서_스마텍.pdf`(11쪽, 2019년 기준)를 홈페이지 내용으로 업그레이드, 업체 제출용 PDF 변환 전제로 HTML 제작.
- 대표님 확정: 주소=홈페이지 기준(수원 본사·천안 수리센터), 공식 대리점=2011 창업부터, 직원 4명, 조직도 영업·설계·관리·AS팀. 배관·설비 공사는 답변 없어 사업영역 카드(Utility System)로만 포함.
- 결과: `바탕화면/회사소개서_스마텍_2026.html`(13쪽, A4 가로, 이미지 내장 1.8MB, 글꼴 Pretendard CDN). 수정용 원본 `바탕화면/회사소개서_작업파일/`(template.html·build.py). 크롬 헤드리스 PDF 변환으로 13쪽 레이아웃 확인.
- 대표님 확인 필요: 인사말 문구, 조직도 팀별 업무 설명(작성자 추정), 자본금 5천만원 현행 여부, 자체 제작품 사진 설명(소음기/트랩 구분 불확실해 묶어 표기), 수리 목록에 Pfeiffer·Leybold 추가(수리키트 DB 근거).

## 2026-09-24 — (클로드) 블로그→X 대기글 연결을 모든 업로드 경로로 확대 (커밋 6303b35)

- 발견: X 대기글 자동 생성이 approve_post.py(수동)에만 연결 → auto_upload.py(월·목 자동 예약, id=100)·upload_post.py 직접(현장사례, id=99)은 누락. 9/28~10/29 10편이 자동 예약이라 전부 누락될 뻔함.
- 수정: upload_post.py 업로드 성공 직후 `generate-x-posts-from-blog.mjs --id` 호출(실패·180초 초과여도 종료코드 유지), approve_post.py 중복 호출 제거.
- 누락분 보충: X#41(id=100 항공우주), X#42(id=99 E2M18) PENDING 생성. id=101 중복 건너뜀 확인.
- 주의: X#41 문구에 "Edwards 공식 데이터시트에…" 사실 주장 포함 → 승인 전 대표님 문구 확인 필요.
- 대표님 결정: 회사소개서 HTML 작업은 보류(당분간 기존 PDF 사용 + 홈페이지 안내).

## 2026-09-24 — (클로드) X 자동게시 대기열 막힘 수정(c129a1c) → 열쇠 없어 자동게시 보류 결정

- c129a1c: 글자수 초과·X 400/403 거절은 그 글만 PENDING+adminNote로 돌려보내고 다음 글 시도, 키없음·401·429·5xx는 상태 유지 후 exit 1(스케줄러에 실패로 표시). dry-run·키없음·가짜키401 시험 통과.
- 열쇠 확보 시도: `vercel env pull --environment=production`으로 X_* 4개 이름은 받았으나 값이 비어 있음(Sensitive 저장 추정, 미확인). 추가 확인은 Claude Code 안전장치가 비밀값 접근으로 차단. 빈 `.env.x.local`은 삭제.
- 대표님: 원본 열쇠 파일 위치를 모름 → **자동게시 보류** 결정. X 사이트에서 재발급 금지(기존 키 무효화 시 수동 "게시" 버튼도 멈춤).
- `SmartechXAutoPublish_Daily` 비활성화는 권한 부족(Access is denied) → 대표님이 관리자 PowerShell에서 `Disable-ScheduledTask -TaskName SmartechXAutoPublish_Daily` 실행 필요. 꺼지기 전까지는 매일 실패 기록만 남고 글 상태는 안 바뀜.
- 게시됨 12건(id 1~11, 20) X 공개 oEmbed로 전부 200·@smartechvacuum 계정 일치 확인.

## 2026-09-24 — (클로드) 원가·내부정보 노출 재발방지 자동검사 (커밋 a3ce1c8)

- `scripts/check-public-leaks.mjs` 신규: ①기본=라이브(또는 `--base`) 공개 API 7개·페이지 6개를 비로그인으로 열어 costPrice·basePrice·tier2/3Price·supplierName·passwordHash·adminNote·selectedExtrasJson 검사 ②`--static`=원가·개인정보 다루는 공개 route 16개의 해시를 `scripts/public-api-reviewed.json`과 대조, 바뀌면 실패 ③`--approve --base 로컬`=실검사 통과 시에만 해시 등록.
- `.git/hooks/pre-push`(로컬 전용, git 미추적)에 `--static` 연결 → Claude·Astra·대표님 누가 push해도 동작. a3ce1c8 push 때 실제로 실행·통과 확인.
- 검증: products route에 costPrice를 일부러 되살려 --static "변경됨" 차단 + 로컬 실검사 "노출 4건" 확인 후 원복.
- 등록 전 16개 파일 직접 검토 중 고객 본인 응답 노출 3곳 추가 수정: /api/quote/[id]/detail(품목 Product 전체→품번·품명·카테고리), /api/repair GET(adminNote·selectedExtrasJson 제외), /api/repair/[repairNo](비관리자 adminNote·키트원가 제외). 로그인 필요 경로라 로컬에서는 401만 확인, 로그인 상태 실확인은 미실시.
- CLAUDE.md·astra_handoff.md 보안수칙에 규칙 추가. Vercel success, 라이브 검사 13/13 통과.
- 한계: 검사 목록에 없는 새 공개 주소는 --static의 "미검토" 감지에 의존. 페이지 HTML 검사는 목록 6곳만.

## 2026-09-24 — (클로드) 🔴 수리 키트 API 원가·마진 노출 수정 (커밋 a528d9d)

- 공개 API 점검 중 발견: `/api/repair/kits`가 비로그인에게 수리 원가(basePrice 30종)·추가부품 원가(41개)를 보내고 /repair 페이지가 브라우저에서 마진(1.5/1.8, 1.2)을 곱함. 2026-05-21 최초 구현부터.
- 수정: 관리자 외에는 서버 계산 customerPrice만 전송, RepairPageClient는 그 값 사용(마진 코드 제거). 관리자 오프라인수리 화면은 원가 그대로.
- 검증: tsc 통과(eslint 오류 5개는 수정 전부터 존재), 로컬에서 76개 고객가 수정 전과 전부 일치, 라이브 반영 후 원가 필드 0개·/repair 200 확인.
- 점검 결과: /api/admin/* 55곳 모두 getAdminSession 등 인증 있음, 라이브 비로그인 8곳 403 확인. 공개 33곳 인증 "작동" 여부 정밀점검은 미실시.
- 남은 일: 재발방지 자동검사, 공개 기능 보안점검, /api/upload/business 무인증(실효 낮음), X 자동게시 실패.

## 2026-09-24 — (클로드) 🔴 제품 API 원가 노출 보안 문제 수정 (커밋 24ddac6)

- 코드리뷰 백로그(34커밋, origin/reviewed..5a7f27d) 검토 중 발견. `/api/products`가 `...p`로 Product 전체 필드를 응답해 **로그인 없이도 전 제품 1,030개의 원가(costPrice)·공급처·재고 등**이 원본 응답(JSON)에 포함됨. 화면에는 표시 안 됨. 2026-04-19 최초 커밋부터 존재 → 6/6 오픈 이후 계속 노출. 대표님이 브라우저로 직접 확인함.
- 수정: costPrice·supplierName·department·minStock·orderQty는 세션 tier=ADMIN일 때만 포함, 그 외엔 displayPrice만. stock은 PumpSelector "국내 재고 보유" 표시에 쓰여 유지.
- 검증: tsc 통과, 로컬 dev 비로그인 원가 0/1030, 라이브 배포 후 동일 확인(원가 0/1030, 판매가 1030/1030). 관리자 원가 경로(발주서 이력불러오기)는 로그인 필요해 로컬 미검증 → 대표님 실사용 확인 필요.
- 다른 공개 API(chat, quote, quote/request, products/prices)는 서버에서 판매가만 계산해 보내 노출 없음 확인.
- 같은 리뷰의 기타 발견: ① X 자동게시(SmartechXAutoPublish_Daily)가 9/23·9/24 모두 "X API 키 없음"으로 실패(로컬 .env에 X_* 없음, 수동 버튼 게시 12건은 Vercel 키로 정상). 스케줄러 결과코드는 0이라 성공처럼 보임 ② 가장 오래된 승인글이 계속 실패하면 뒤 글이 전부 막히는 구조 ③ 발주서 25개↑ 원가 0원 → 다른 세션이 354acb1로 이미 수정.
- reviewed 포인터는 아직 이동 안 함(X 자동게시 문제 처리 후 이동 예정).

## 2026-09-23 — (클로드) X 콘텐츠 승인글 하루 1개씩 자동 게시 (커밋 a450ca1)

- 대표님이 "승인된 글들을 매번 내가 직접 게시하고 있다"며 자동화 요청. 기존 `/admin/x-posts` 흐름을 확인하니 APPROVED까지만 자동이고 실제 X 게시는 항상 관리자가 "게시" 버튼을 눌러야 했음(`app/api/admin/x-posts/route.ts`).
- 이 컴퓨터에서 자동 실행(Windows 작업 스케줄러) vs Vercel 24시간 서버 실행 중 선택 요청 → "이 컴퓨터에서 자동 실행" 선택받음(vercel.json은 보호파일이라 서버쪽은 추가 승인 필요했을 것).
- `scripts/auto-publish-x-post.mjs` 신규 작성: `route.ts`의 publish 로직(원자적 APPROVED→PUBLISHING 선점, 글자수 280자 검사, 게시확인실패시 PUBLISHING 잠금 유지)과 `lib/x-post.ts`의 OAuth 1.0a 서명 로직을 인라인 복제(`.mjs`에서 `.ts` 직접 import 불가 — `generate-x-posts-from-blog.mjs`와 동일 기존 패턴). `--dry-run` 옵션으로 실제 게시 없이 검증 가능.
- `--dry-run`으로 먼저 검증(승인대기 27건 중 가장 오래된 id=12를 정확히 선택 확인) 후 실제 게시 코드는 아직 실행하지 않은 채 스케줄러만 등록.
- `scripts/run-x-auto-publish.bat`(한글 없음, 인코딩 문제 회피) + `scripts/setup-x-auto-publish-scheduler.ps1`(블로그 `setup_scheduler.ps1`과 동일 패턴: KST 시간대 검사, Hidden, 기존 작업 XML 백업) 작성.
- 스케줄러 등록은 관리자 권한 필요해서 최초 시도(`Access is denied`) → 대표님이 관리자 PowerShell 직접 열어 재실행 → `SmartechXAutoPublish_Daily` 등록 확인(State=Ready, NextRunTime=2026-09-23 11:00).
- 매일 11:00(KST) 승인글 중 가장 오래된 1건 자동 게시, 실패 시 로그(`x-auto-publish.log`, git-ignored)에 남기고 APPROVED로 되돌려 다음날 재시도. 게시 확인 실패(PostedButUnconfirmedError)는 PUBLISHING 잠금 유지해 중복게시 방지.
- 커밋 `a450ca1` push, `git ls-remote`로 원격 일치 확인. 이 변경은 Vercel 앱 코드가 아니라 로컬 스크립트라 Vercel 배포와 무관.
- **다음 확인 필요**: 오늘(2026-09-23) 11:00 실제 첫 자동 게시가 성공했는지 `x-auto-publish.log`와 `/admin/x-posts` POSTED 탭에서 확인.

## 2026-09-20 — (클로드) 현장사례 발행 — 0918-Y대-E2M18 (id=99)

- 대표님이 데스크톱 사진 1장(`E2M18_납품사.jpg`)과 함께 "진공오븐용 E2M18 납품, 기존 RV12 용량부족으로 업그레이드" 구두 정보 제공. 날짜(9/18)와 학교명(연세대 첨단과학기술연구관, 리서치 참고용·비공개)을 확인 질문으로 받아 `블로그/data/cases/20260918_납품_Y대_E2M18/`에 케이스 등록.
- `블로그/CLAUDE.md` 현장사례 파이프라인대로 research.md(내부 Product Master Table + Edwards RV/E2M 공식페이지) → 5종 콘텐츠(현장사례 카테고리) → 이미지(사진 1장뿐이라 thumbnail=원본 전체컷, 나머지 2장은 원본 크롭+RV12 공식 참고이미지 크롭으로 구성, 학교 식별정보 없음 확인) → check_quality.py 순으로 진행.
- 사진이 1장뿐이라 field-*.png 체크(현장사진 존재 판정)와 사진N.png 체크(마커 대조)가 서로 다른 파일명을 요구해 처음에 반려됨 — blur-config.json에 blur_radius=0·source_image_size 명시해 두 체크를 동시에 만족시키는 방식으로 해결(향후 사진 1장짜리 현장사례에 재사용 가능한 패턴).
- final.md 글자수가 2,000자 기준에 약 700자 부족해 허구 사례 추가 없이 "오일식 유지 이유", "설치 시 확인사항", "재검토 시점" 섹션을 실제 근거로 보강해 통과.
- `upload_post.py`로 큐 등록 없이 직접 업로드(현장사례는 월목 큐 대상이 아님) → https://www.smartechvacuum.com/blog/99 (HTTP 200 확인). 네이버 발행 후 마커/주석 잔존 여부를 `curl`로 원문 대조해 없음 확인.
- 학교명은 info.txt에만 남고 naver.md/final.md/meta.txt/faq.json 어디에도 노출되지 않음.

## 2026-09-17 — (클로드) W39 블로그 2편 원고 완성 — 0921-항공우주-오일로터리, 0924-질소퍼지-안전

- 대표님이 다음주 월·목(9/21·9/24) 주제 후보 4개 요청 → topic-tracker.json(current_industry_index=10)·industry-product-matrix.md 대조해 후보 4개 제시, "2번 4번" 선택받음.
- 두 편을 fork 서브에이전트 2개로 병렬 작성(리서치→글쓰기 5종+final.md→이미지→check_quality.py). 각 fork에 기존 발행글과의 중복 회피·이미지 중복 금지(0914·0917 재발 패턴 주의)·topic-tracker/upload-queue 직접 수정 금지·업로드 스크립트 실행 금지를 명시했다.
- 완료 후 서브에이전트 자기보고를 그대로 믿지 않고 check_quality.py를 직접 재실행해 통과 확인, 두 폴더 이미지 9장 MD5를 서로 대조해 중복 없음도 재검증했다.
- 사진 승인 받은 뒤 upload-queue.json을 W39(day1=0921, day2=0924, approved=true·uploaded=false)로 교체, topic-tracker.json completed_topics에 2건 추가(publication_status=draft), industry-product-matrix.md 항공우주 B칸 ✅ 처리. 일반주제(질소퍼지)는 매트릭스·current_industry_index 변경 없음.
- **다음 행동**: 9/21·9/24 해당일에 `python 블로그/approve_post.py "[폴더경로]"`로 실제 업로드 필요(Windows 자동예약 미적용 상태 지속). 두 원고 모두 아직 미발행.

## 2026-09-17 — (클로드) 목요일 블로그 발행 — 0917-진공-물-끓음 (id=98)

- 대표님 요청 "홈페이지 블로그 글 목요일 업로드 해줄래" → `upload-queue.json` day2(`0917-진공-물-끓음`, scheduled_date 2026-09-17) 확인, 원고·검수는 완료 상태였으나 업로드 전 이미지 전체 MD5 재대조 중 **사진1이 썸네일과 완전 동일 파일**인 문제 발견(0914 때와 같은 재발 패턴). 다른 발행/미발행 글과의 교차 중복은 없음.
- 대표님 승인으로 사진1만 새 이미지로 교체(폴더에 이미 있던 `gen_images.mjs`의 카드 스타일로 재생성, 사진2~4와 톤 통일). check_quality.py 재검사 전체 통과, 사진 재승인 받은 뒤 `approve_post.py`로 발행.
- 결과: https://www.smartechvacuum.com/blog/98 (HTTP 200 확인). X 초안 자동생성도 정상 트리거(관리자 화면 승인 대기).
- Windows 목요일 예약 작업은 여전히 미적용 상태라 이번에도 수동 실행했다. `docs/memory/current-state.md`의 예약 미완료 항목은 그대로 유효.

## 2026-09-15 — (클로드) 발주서에 대행견적 이력불러오기 + 원가 자동기재 (커밋 4342a08)

- 대표님 요청: 대행견적서(예: 인포라드)로 나간 품목을 에드워드(IV/SV/AK)에 발주할 때, 발주서 이력불러오기로 대행견적 품목을 가져오면서 단가가 원가로 자동 기재되게 해달라는 요청. 사전에 요구사항을 문장으로 확인받고("작업은 그 다음에"), "AK" 부서 확정까지 받은 뒤 작업 시작.
- `app/admin/purchase-orders/page.tsx`(IV/SV/AK 공통 폼)에 "대행견적 이력불러오기" 버튼·모달 추가. 기존 대행견적서 화면이 쓰던 `/api/admin/proxy-quotes/history`를 그대로 재사용(업체명 검색).
- 선택한 견적의 품목·수량은 유지하고, 단가는 견적의 고객 청구가가 아니라 `Product.costPrice`로 자동 대체(`/api/products?ids=` / `?partNos=` 재사용). 불러온 뒤 금액 수정·품목 삭제는 기존 편집 UI 그대로 사용 가능.
- 실제 인포라드 견적(quote id=230, 14개 품목)으로 직접 재현 검증하던 중, 품목 하나의 견적 당시 저장 파트번호(`D14641000`)가 이후 카탈로그 정정으로 현재 실제 파트번호(`D14642000`)와 달라져 있는 걸 발견. 문자열 매칭만 썼다면 이 품목은 원가 0원으로 잘못 채워졌을 상황.
- 이를 `QuoteItem.productId`(문자열과 무관하게 안정적인 FK) 우선 매칭으로 해결. `/api/admin/proxy-quotes/history` 응답에 `productId` 필드를 추가했고(기존 대행견적서 화면의 소비 로직엔 영향 없는 additive 변경), 매칭되면 발주서 품목의 파트번호도 현재 카탈로그 값으로 갱신해서 잘못된 품번으로 발주 나가는 걸 막음.
- 검증: `tsc --noEmit` 통과, `eslint` 신규 오류 0(이 파일은 기존에 `react-hooks/set-state-in-effect` 미해결 5건이 있었고 그대로 유지, 내가 추가한 useEffect도 동일 패턴 회피하도록 구조 조정), `next build`(schema 변경 없어 `prisma generate`의 상습 EPERM을 피해 next만 재실행) 통과. 실제 라이브 `/api/products`를 그대로 호출하는 시뮬레이션 스크립트로 14개 품목 전부 원가 매칭(unmatchedCount=0) 확인.
- 커밋 `4342a08` push, `git rev-parse HEAD`/`git ls-remote`로 원격 일치 확인. `vercel ls`로 Production 배포(`smartech-93l36mo8t`)가 Building→Ready로 전환되는 것까지 폴링 확인, 배포 후 `/api/products?ids=707`로 라이브에서 실제 정정된 파트번호·원가가 나오는지 최종 재확인.

## 2026-09-15 — (클로드) 블로그 발행 → X 초안 자동생성 연결 (커밋 1a3411a)

- 대표님이 "새로 생성되는 블로그에 맞춰 X 글도 초안 작성해서 게시하면 좋겠지?"라고 물어, "발행 시 초안만 자동 생성하고 게시는 계속 수동 승인"으로 제안 → "연결하자" 승인받고 진행.
- `scripts/generate-x-posts-from-blog.mjs`에 `--id 123` 옵션 추가: 블로그 글 1건만 지정해 X 초안 생성(기존 무작위 N건 배치 모드는 그대로 유지), 이미 사용된 id는 스킵.
- `블로그/approve_post.py` 수정: 기존엔 `subprocess.run`으로 `upload_post.py`를 실행하며 출력을 캡처하지 않았음. `Popen`으로 바꿔 콘솔에 실시간 출력은 그대로 하면서 `"[SUCCESS] id="` 줄을 정규식으로 파싱해 새로 발행된 블로그 id를 얻고, 업로드 성공 직후 `node scripts/generate-x-posts-from-blog.mjs --id {새id}`를 자동 호출.
- 안전장치 유지: X 게시 자체는 자동화하지 않음. 생성된 초안은 여전히 PENDING 상태로 쌓이고 관리자가 `/admin/x-posts`에서 직접 승인·게시 버튼을 눌러야 실제 게시됨. X 초안 생성이 실패해도 블로그 발행 자체는 영향받지 않도록 예외 분리(WARN 로그만 남기고 종료코드 실패 처리 안 함).
- 검증: 실제 approve_post.py 전체 실행은 하지 않고(중복 발행 위험 회피), 동일한 subprocess 호출 방식을 별도로 시뮬레이션해 blog id=76 기준 정상 생성 확인. `--id` 옵션의 dedup(이미 사용된 id 스킵)·not-found(존재하지 않는 id) 케이스도 개별 테스트로 확인.
- `node --check`·`eslint`·`python -m py_compile` 모두 통과 확인 후 커밋·push, `git rev-parse HEAD`/`git ls-remote`로 원격 일치 확인.

## 2026-09-15 — (클로드) X 글자수 버그 코드 수정 + 잔여 초안 5건 축약 (커밋 cb8b672)

- 아래 Astra 조사·시험게시 성공을 이어받아 클로드가 코드 수정. `lib/x-text-length.ts`에 X 공식 weightedLength(한글·CJK·전각 2자) 계산 함수 추가.
- 적용 범위 3곳: `app/api/admin/x-posts/generate/route.ts`(초안 생성 시 검사), `app/api/admin/x-posts/route.ts`의 PATCH publish 액션(게시 직전 사전검사 — 초과 시 400으로 막아 X에 보내지 않음), `app/admin/x-posts/page.tsx`(상세 패널 글자수 표시, 초과 시 빨간색).
- DB에서 PENDING·APPROVED 잔여 5건(id 1~5)을 조회해 전부 X 기준 초과임을 확인(예: id=3 화면 226자 vs 실제 388자). 각 글의 핵심 메시지는 유지하고 문장만 줄여 content 필드만 직접 수정(topic·status 등 다른 필드 불변), 축약 후 전부 280자 이내로 재검증.
- `npx tsc --noEmit` 통과 확인 후 커밋(최초 커밋에 attribution 누락 발견해 amend로 정정) → `git push origin master` → `git rev-parse HEAD`와 `git ls-remote origin refs/heads/master` 대조로 원격 반영 확인.
- 미확인 사항: 실제 "게시" 버튼 클릭으로 5건이 정상 게시되는지는 대표님 확인 필요(코드상 검증은 완료, 실게시 테스트는 남음).

## 2026-09-15 — X 게시 403 재조사·기존 글 1건 축약 후 게시 성공

- Claude 원본 `project_x_grok_content.md`와 GitHub X 연동 이력을 확인했다. 당시 읽기·쓰기 권한, 토큰 재발급, 크레딧 충전, 새 앱 생성 후에도 403이 지속되어 보류한 기록이 있다. 과거의 "X 플랫폼 버그 확정"은 현재 확정 원인으로 취급하지 않는다.
- 현재 초안 생성 검증과 관리자 길이 표시는 `content.length`를 사용한다. X 공식 한글 가중치(2)를 반영하지 않아 화면상 247자인 정기점검 글은 실제 X 기준 409자다. 이 결함과 403의 직접 인과관계는 아직 미확인이다.
- 대표님이 기존 글 한 건을 줄여 저장하고 게시 시험하도록 승인했다. DB에서 제목·원문·상태가 화면과 일치하는 XPost id=6을 확인하고, 원문을 `tools/x-post-6-before-shortening.json`에 로컬 백업했다(git 제외).
- id=6 본문만 앞서 제시한 축약안으로 저장했다. 원문·승인 상태·수정 시각을 조건으로 한 갱신 결과 1건, 별도 DB 재조회로 저장 확인. 일반 길이 139자 / X 가중 길이 226자. 상태 APPROVED 유지, tweetId·postedAt 없음. 다른 글은 변경하지 않았다.
- 홈페이지 관리자 인증 확인이 실패하여 실제 publish 요청은 한 번도 보내지 않았다. 로컬 임시 진단 스크립트는 제거했고 원문 백업은 보존했다. X 인증키를 재발급하거나 환경변수·홈페이지 코드를 수정하지 않았다.
- 대표님이 수정된 글을 직접 게시한 뒤 성공을 알렸다. DB 재조회에서 id=6 상태 POSTED, tweetId `2099639323054448660`, postedAt `2026-09-14T23:19:58.880Z`(한국 9/15 08:19:58)를 확인했다. 게시 URL: https://x.com/smartechvacuum/status/2099639323054448660 . 공개 페이지는 별도로 조회하지 않았다.
- 결론: 동일 앱·키·게시 코드로 축약 글 게시에 성공했으므로 현재 게시 연동이 작동함을 확인했다. 한글 길이 계산 오류와 기존 글의 제한 초과는 확정이며, 이번 성공은 길이 문제 가설을 강하게 지지한다. 본문도 함께 변경됐으므로 과거 모든 403의 단일 원인까지 입증했다고 단정하지 않는다.
- 후속 제안: 초안 생성·화면 표시·게시 전 검사를 X 가중 글자 수에 맞춰 수정해 재발 방지. 이번 승인 범위는 기존 글 한 건 축약·게시 시험이며, 해당 코드 수정과 나머지 글 수정·게시에는 아직 착수하지 않았다.

## 2026-09-14 — EMF20/EMF3 단가 확인 (사용자 DB 직접 수정, 코드 변경 없음)

- 대표님이 관리자 화면에서 EMF20·EMF3 등 일부 품목 단가가 높게 세팅돼 있는 것을 발견해 직접 DB에서 수정. 코드/git 변경 없음.
- 과거 버그 기록 확인: 2026-06-18 "전체검색 하드코딩 가격" 버그(커밋 fe71196)를 고칠 때 "카테고리 선택 후 세부 검색(`productItems`)은 여전히 JSON 가격 사용"이 미해결로 남아있었음.
- `lib/productCatalog.json`을 직접 확인한 결과, `components/PumpSelector.tsx`의 세부 검색 경로가 지금도 이 파일의 고정가를 그대로 쓰고 있고, EMF3(A46220000) 202,500원·EMF20(A46229000) 310,500원이 박혀 있음을 확인. 오늘 DB 수정값이 반영 안 되는 경로가 실제로 남아있다는 뜻.
- 이후 대표님이 "오늘 세팅한 금액을 최종으로 남기고 근본적으로 해결하자"고 결정 → JSON 값 수동 삭제 대신 코드 수정으로 근본 해결 진행.

### 근본 수정 — 카테고리별 세부검색 가격을 DB 실시간 조회로 전환 (커밋 `3f83979`)

- `app/api/products/route.ts`: `partNos`(콤마구분 파트번호 다건조회) 쿼리 파라미터 추가.
- `components/PumpSelector.tsx`: "카테고리 선택 → 세부검색" 목록(`productItems`)이 `lib/productCatalog.json`의 고정 `price`를 직접 읽던 것을 제거. JSON은 이제 파트번호·품명 구조만 제공하고, 가격은 카테고리 선택 시 `/api/products?partNos=...`로 실시간 조회해 표시(로그인 등급별 마진율 자동 반영, 미표시 대상은 "문의").
- 이 화면은 EM(미스트필터) 계열뿐 아니라 JSON에 정의된 모든 카테고리(오일펌프·스크롤펌프·게이지 등)에 공통 적용되는 컴포넌트라, 이번 수정으로 EMF20/EMF3 외 다른 카테고리의 동일한 잠재 오류도 함께 해소됨.
- 실제 장바구니·견적서 금액은 이전부터 DB 가격 기준이었음(파트번호로 상품 조회 후 별도 계산) — 이번 문제는 "목록 화면 표시 가격"에 한정됐고 고객이 실제로 잘못된 금액을 결제한 사례는 아니었음.
- 검증: `tsc --noEmit` 통과, `eslint` 신규 코드 0 error(사전 존재하던 무관한 에러 1건은 그대로 둠, 범위 밖), `next build` 전체 성공. 로컬 프로덕션 서버에서 `/api/products?partNos=A46229000,A46220000`·`?q=EMF20` 모두 정상 응답 확인.
- 커밋 `3f83979` push 완료, 원격 해시 일치 확인. 라이브 배포(Vercel) 완료 후 `https://www.smartechvacuum.com/api/products?partNos=A46229000,A46220000`로 재확인: EMF3 원가 150,000→표시가 232,500원, EMF20 원가 230,000→표시가 356,500원 정상 반영(`total:2`로 필터 정확히 동작).
- 관련 과거 미해결 항목: [제품 검색 가격 버그 수정](메모리 `project_pricing_fix.md`)의 2026-06-18 "카테고리 선택 후 세부 검색(productItems)도 여전히 JSON 가격 사용 중" 항목 — 이번 커밋으로 완전히 해소.

## 2026-09-14 — 0914-반도체-전해연마-배관 월요일 발행 (id=97)

- 대표님이 "월요일 블로그 업로드하자" 요청. 사진 승인(Step 4.5) 재확인 중 사용자가 사진1이 썸네일과 똑같다고 지적.
- 확인 결과 `thumbnail.png`와 `사진1.png`가 MD5 완전 일치(바이트 단위 중복, 1,955,594바이트) — 6단계 "복사 대신 이동" 규칙 위반과 동일 유형의 재발 사고(과거 2026-08-08 `0814-재가동전점검사항` 사례와 같은 패턴). 사진1을 `gen_images.mjs`에 정의된 표면처리 전/후 비교 카드로 새로 생성.
- 사용자가 사진3도 다른 글과 중복 아니냐고 재확인 요청 → 전체 `output/` PNG MD5 대조 결과, 사진3이 `0911-아웃가스-원리`(미발행)의 사진3과 완전히 동일한 파일(`public/images/products/hardware.png` 복사본, 재사용 금지 규칙 위반)로 확인. "시험 항목 구분(표면처리/누설시험/파티클검사/베이스압력)" 카드로 새로 생성하고, final.md·naver.md의 캡션도 실제 이미지 내용에 맞게 수정.
- 두 교체 모두 `check_quality.py` 재검증 통과(2,043자, 이미지 마커 3개=파일 3개 일치) 후 사용자에게 재승인(OK) 받음.
- `python 블로그/approve_post.py "기술블로그/2026-09/0914-반도체-전해연마-배관"` 실행 → 홈페이지 업로드 성공 `id=97`, `https://www.smartechvacuum.com/blog/97`. `upload-queue.json` day1 `approved=true`/`uploaded=true` 자동 기록됨.
- 라이브 URL curl 확인 HTTP 200. 커밋·push는 별도 진행 필요(이번 세션은 이미지 파일·캡션 텍스트만 변경, 코드 변경 없음).
- 남은 큐: day2 `0917-진공-물-끓음`(목요일)은 아직 미승인. 발행 전 같은 방식(썸네일·사진1~N 전체 MD5 상호 대조)으로 중복 여부 재확인 필요 — 이번 사고가 image-maker 6단계 절차 준수만으로는 막히지 않았으므로, 다음 발행 때도 습관적으로 대조할 것.

## 2026-09-12 — 다음 주 월·목 블로그 원고 2편 작성·검수 완료

- 대표님이 선택한 `0914-반도체-전해연마-배관`과 `0917-진공-물-끓음`을 각각 월요일·목요일 원고로 작성했다.
- ASTM B912, Pfeiffer Vacuum, Edwards 공식 자료와 NIST 물성 자료를 대조해 리서치 파일에 기록하고, 핵심 근거 링크를 원고에 반영했다.
- 두 글 모두 설명 도식과 썸네일을 제작·직접 확인했다. 정확한 미사용 현장사진이 없어 제품 참고 이미지 또는 설명 도식을 사용했으며, 현장사진으로 소개하지 않았다.
- `python check_quality.py` 검사 결과 두 폴더 모두 전체 통과: 4번 2,043자·이미지 3개, 5번 2,059자·이미지 4개. 표 문법·메타·네이버 원고·이미지 용량 검사도 통과했다.
- 작성완료 이력과 반도체 E항목 상태를 갱신했다. `current_industry_index`와 상담기록 사용 이력은 변경하지 않았다.
- 사진·원고 승인, W38 큐 등록, 예약 작업 적용, 홈페이지 발행은 아직 진행하지 않았다.

## 2026-09-12 — 블로그 대표 이미지 교체

- 사용자가 기존 카드형 이미지가 마음에 들지 않는다고 피드백해, 두 글의 썸네일과 첫 본문 이미지를 진공 배관·챔버·압력계가 보이는 AI 생성 기술 일러스트로 교체했다.
- 생성 이미지는 직접 시각 확인했고, 실제 현장사진이 아닌 설명용 이미지라는 기록을 각 `no-field-photo.md`에 반영했다. 나머지 본문 도식은 유지했다.

## 2026-09-11 — 블로그 자동 업로드 검증 및 수동 운영 결정

- `uploaded=None`을 미업로드로 처리하도록 한 수정의 테스트 기대값을 바로잡았다. `None`은 업로드 1회, 문자열 `"true"`는 오류 종료로 검증했다.
- Python 문법 체크, 자동 업로드 안전성 테스트 7개, PowerShell 스케줄러 WhatIf 계약 테스트를 모두 통과했다.
- 자동 업로드 코드·스케줄러 설정·검증 테스트 4개 파일을 `a92a643`으로 로컬 커밋했다. 기존 미커밋 문서·콘텐츠 변경은 포함하지 않았다.
- 당분간 Windows 예약 작업은 적용하지 않고, 사용자가 요청하는 월요일/목요일에 `--slot day1`/`--slot day2`로 수동 업로드한다. 자동화 전환 시 `setup_scheduler.ps1 -WhatIf` 확인 후 관리자 권한으로 적용한다.
- 오늘은 메모리 기록을 추가한 뒤 push하며, 예약 작업 자체는 실행하지 않는다.

## 2026-09-11 — 주문 확정 시 담당자 연락처 필수 입력 (Claude)

- 계기: 대표님이 홈페이지 주문 목록에서 김을수(엘케이엔지니어링) 주문건에 연락 가능한 정보가 사업자번호뿐이라 확인 요청.
- 원인 확인: `app/quote/[id]/page.tsx`의 정상 주문 확정(`handleConfirm`)은 `contactName`/`contactPhone` 없이 `quoteId`만 전송. 연락처 입력창은 "납기문의"를 선택했을 때만 나타나던 구조였음(백엔드 `/api/orders`는 원래부터 두 값을 받아 저장하도록 되어 있었음, 코드 변경 불필요).
- 조치: 납기 확인 모달의 "선택" 화면에도 이름·전화번호 입력칸을 추가하고, 회원 정보(`quote.user.name`/`phone`)로 기본값을 채운 뒤 확인 전 필수 검증을 걸었다. 주문 확정 API 호출에도 두 값을 함께 전송하도록 수정.
- 검증: `npx tsc --noEmit`으로 대상 파일 타입 오류 없음 확인, `npx next build` 전체 성공.
- 커밋 `c9f8785` (1개 파일만 수정), master push 완료·원격 해시 일치 확인. Vercel 실제 배포 상태는 별도 확인 필요.
- 남은 과제: 김을수 주문(#6)은 이미 연락처 없이 접수된 건이라 소급 반영 안 됨 — 필요 시 직접 연락해서 확인해야 함.
- 로컬 코드리뷰(무료) 실행 결과 `블로그/auto_upload.py`에서 별건 버그 발견: `uploaded` 키가 누락된 경우(수동 큐 편집 실수) 예전엔 "미업로드"로 처리했는데, 최근 미커밋 수정이 이를 치명적 오류로 바꿔 예약 발행이 조용히 중단될 위험이 있었음. `uploaded is True`(이미 완료) / `uploaded not in (False, None)`(잘못된 값만 에러) 조건으로 수정해 원래 관대한 동작을 복원. 문법 체크·조건 로직 4케이스 검증 완료.
- **이 수정은 대표님 지시로 커밋하지 않고 로컬에 남겨둠**("그냥 두고 마무리 하자", 2026-09-11). 같은 파일에 이전 세션의 월·목 전환 미커밋 변경도 섞여 있어 분리 커밋 불가. 다음 세션에서 이 파일 전체를 검토 후 한 번에 커밋할지 결정 필요.

## 2026-09-09 — 0911-아웃가스-원리

- 사용자 승인 주제: “진공 챔버에서 계속 나오는 가스는 어디서 올까? — 아웃가스의 원리”
- 공식 기술 자료 8개를 바탕으로 리서치 완료. 아웃가스(탈착·확산·기화), 실제 누설, 가상 누설, 투과를 구분하고 일률적인 가열 조건은 제시하지 않음.
- 산출물 폴더: `블로그/output/기술블로그/2026-09/0911-아웃가스-원리/`
- 작성 파일: `final.md`, `google.md`, `naver.md`, `meta.txt`, `faq.json`, `marketing.txt`
- 이미지 5개 제작·시각 확인 완료. 현장사진 대신 설명 도식 4개와 부품 참고 이미지 1개를 사용했으며, `no-field-photo.md`에 사유 기록.
- 품질 검사: `python 블로그/check_quality.py "기술블로그/2026-09/0911-아웃가스-원리"` 전체 통과. final.md 2,010자, naver.md 2,067자, 이미지 마커 4개와 사진 파일 4개 일치.
- 업로드·사진 승인·금요일 예약 등록은 아직 하지 않음. 사용자가 사진과 내용을 확인한 뒤 별도로 진행.

## 2026-09-09 — 업로드 진행 기준 합의

- 사용자가 금요일에 “블로그글 업로드해줘”라고 말하면 `0911-아웃가스-원리` 글을 홈페이지 블로그에 업로드한다.
- 업로드 전 사진과 최종 원고를 확인하고, 별도 사진 승인이 필요한 경우 그 절차를 먼저 진행한다.

## 2026-09-09 — Astra 운영 이관·SEO/GEO 점검·월목 전환

### 승인 범위

대표님이 외출 중에도 꼼꼼히 이어서 작업하도록 승인. Codex 중심 운영으로 전환하고 규칙·메모리·커밋/push 절차를 검토하며 홈페이지 전반 SEO/GEO 점검과 다음주 후보 5개를 준비한다. 다음주 두 글은 선택을 기다리고 금요일 글은 그날 업로드 요청을 기다린다.

### 완료한 변경

- Claude 메모리 Markdown 97개를 비공개 로컬 스냅샷으로 보존하고 파일별 SHA-256 일치 확인. 원본 수정 없음. `docs/memory/README.md`, `current-state.md`, manifest를 새 세션 시작점으로 연결했다.
- root CLAUDE·handoff·README 및 블로그 규칙/에이전트/가이드 9개를 대조·정리. 보호 파일 11개·색상 8개·패키지 규칙 유지. 실제 파일 접근과 자동 기억/훅 실행을 구분하고 커밋→검토→push→배포 확인·기록 책임을 통일했다.
- README의 사용하지 않는 Geist 설명을 제거하고 과거 디버깅/디자인 제안에는 역사 기록임을 표시했다.
- 출처 표시 금지, 허구 경력·사례 유도, 기술 조건을 제거하는 단정, 근거 없는 SEO 수치·FAQ 혜택을 정리했다. 최신 공식 Google/네이버 자료와 내부 편집 규칙을 구분했다.
- 월·목 전환 Python/PowerShell 코드 및 검증 준비. 날짜·주차·승인·발행 상태를 점검하며 미래·과거 큐의 오발행을 방지한다. 기존 큐와 승인 값은 변경하지 않았다.
- tracker 누락 원고 4개를 작성완료로 보완(46→50). 공개 미확인은 unverified, 금요일 원고는 draft. 매트릭스 누락 12칸도 기존 원고와 대조해 보완했다. 순환 인덱스 10과 상담기록은 유지했다. 다음주 후보는 완료/예약 처리하지 않았다.
- `0911-아웃가스-원리`: 반복 문장 교정, 공식 출처 8개, final/google 동일, final 실질 2,294자·네이버 실질 2,179자·메타 150자·FAQ 6개. 기존 이미지 5개 유지. 기존 2,010자 검사 통과 기록보다 이번 교정본이 최신이다.
- 블로그 공통 표시: Markdown 공식 자료/내부 링크를 클릭 가능하게 처리하고 React 텍스트로 HTML 삽입을 방지했다. 중복 제목·내부 편집 주석을 제거하고 JSON-LD의 `<`를 이스케이프했다. 기존 DB 글 본문·URL은 수정하지 않았다.
- `seo-check`의 “전체 SEO 정상” 표현을 선언 검사로 한정하고 client 파일의 metadata 혼용도 실패로 처리했다.
- `.gitignore`에 하위 Claude 로컬 설정·Windows 작업 XML 백업·비공개 메모리 스냅샷을 제외했다. 기존 로컬 설정 파일 자체는 수정하지 않았다.

### 홈페이지 점검 결과

- 공개 사이트맵 1,162개, 중복 0개. 공개 블로그 54개 포함 표본 102개 모두 HTTP 200. HTTP 검사와 검색 색인 확인은 별개다.
- 최초 공개 표본에서 블로그 중복 H1 52개, 블로그 본문 링크 0/54, description 누락 1개, 제품 목록 초기 HTML H1 부재, 제품 InStock 고정 및 공통 FAQ 불일치 위험 확인.
- Lighthouse 13.4.1: 홈 모바일 성능55/접근성97/기술준수100/SEO100, LCP11.2초. 홈PC84/97/100/100, LCP2.8초. 블로그96 모바일84/96/100/100, LCP4.1초. 각1회 실험실 측정이며 실제 사용자·순위·GEO 점수가 아니다.
- [사이트 점검 보고서](audits/2026-09-09-seo-geo-review.md), [기존 글·후보 보고서](audits/2026-09-09-blog-review.md), 측정 HTML/JSON을 저장했다.
- 기존 글의 기술 오류·내부 모순·근거 누락을 우선 교정하도록 10편 공개 URL과 남아 있는 문제 문장을 대조했다. 아직 해당 10편 DB 원고를 일괄 수정하지 않았다.

### 검증

- `python -B -m unittest discover -s 블로그 -p test_auto_upload_schedule.py -v`: 6개 그룹/25개 시나리오 PASS.
- `powershell -NoProfile -ExecutionPolicy Bypass -File ./블로그/test_setup_scheduler.ps1`: 모의 예약/백업/비활성화 검사 PASS, 실제 예약과 DB 변경 없음.
- `node node_modules/tsx/dist/cli.mjs scripts/test-blog-inline.tsx`: 링크·강조·HTML 이스케이프·위험 URL·중복 제목/주석 검사 PASS.
- `node node_modules/typescript/bin/tsc --noEmit --incremental false`: PASS.
- 수정 TSX 2개 ESLint: PASS. 기존 페이지의 등록되지 않은 Next lint rule 비활성화 주석 3개를 제거했다. 전 저장소 lint 통과를 주장하지 않는다.
- `npm.cmd run seo-check`: 13개 페이지 PASS.
- `npm.cmd run build`: Prisma DLL 교체가 EPERM으로 실패. 샌드박스 밖 재시도도 같았다. 기존 실행 프로세스는 중단하지 않았다.
- 스키마를 바꾸지 않았으므로 기존 Prisma client로 `node node_modules/next/dist/bin/next build` 실행: 컴파일·타입·130개 정적 페이지 생성 PASS.
- 임시 로컬 production 서버의 `/blog/96` HTTP200: H1 2→1, title/canonical 동일, 이미지3→3, JSON-LD 구문오류0. 서버의 처음 DB 접근은 샌드박스 네트워크에서 실패해 권한 있는 읽기 전용 검증으로 재실행했다. [검증 JSON](audits/2026-09-09-local-blog-verification.json).
- tracker 작성 원고50개, 폴더 중복0, 기술 원고 누락0 확인. 원고 출판 여부는 별도다.

### Windows 예약 적용 미완료

- 실제 `-WhatIf` 계획 확인 성공. 기존 월·수·금 작업 XML3개 백업 완료.
- 실제 스크립트는 첫 Register-ScheduledTask에서 OS `Access is denied (0x80070005)`로 중단. 자동 승인 검토 거절이 아니라 Windows 관리자 권한 부족이다.
- 기존3개 작업이 그대로이고 목요일 새 작업이 없음을 재조회했다. 새 코드만으로는 목요일 자동 실행이 생기지 않는다.
- 대표님 복귀 후 관리자 PowerShell에서 준비된 스크립트 실행·작업 조회가 남는다. 큐를 미리 채우거나 글을 승인하지 않았다.

### 남겨둔 상태

- 금요일 글과 다음주 2편의 사용자 선택/원고·사진·발행 승인은 대기.
- 검색 실적·AI 인용·문의 전환의 최신 계정 데이터는 미확인. 다음 개선은 기술 오류가 있는 기존 글과 모바일 첫 화면 속도가 우선이다.
- 작업 시작 시 master는 원격과 동일했고 reviewed 백로그는10개였다. 이번 변경 검토만으로 과거10개 전체를 검토한 것은 아니므로 reviewed를 임의 전진시키지 않는다.
- `블로그/quality-log.jsonl`과 `upload-log.txt`에는 시작 전/자동 실행 기록이 섞여 있어 파일 전체를 이번 커밋에 포함하지 않는다. 이번 검사 결과는 원고별 검증 메모와 이 로그에 별도로 기록했다.
- 커밋·push·실제 배포 결과는 아래 후속 항목에 기록한다.

## 2026-09-10 — 홈페이지 SEO·GEO 공통 오류 개선

- 대표님이 9/9 홈페이지 진단에 따른 개선 진행을 승인했다. 홈페이지 설명문 표시 지연·제품 카드 이미지 전달·제품 목록 초기 제목·고정 재고/FAQ 검색 정보·블로그 설명/대표 이미지를 개선했다.
- 9/9 미반영 블로그 공통 링크·중복 제목 처리와 SEO 검사 문구도 검토해 이번 홈페이지 코드에 포함한다. 기존 블로그 원고·자동 발행·Windows 예약 변경은 포함하지 않는다.
- 변경 전 파일을 `tools/seo-backup-2026-09-10/`에 백업했다. 보호 파일 11개 변경 없음.
- 최종 `npm.cmd run build` 성공(Prisma 생성·타입 검사·정적 페이지 130개). 별도 타입 검사·13개 SEO 선언 검사·블로그 링크/설명/이미지 검사 통과. 변경 TS/TSX ESLint 오류 0, 기존 미사용 import 경고 1.
- 최종 로컬 production 브라우저: 주요 5개 페이지 HTTP 200, H1 각 1개, 제품 목록 초기 HTML H1 존재, 요약 설명 누락 보완, 재고 고정/공통 FAQ 제거, RV12 제품 검색 정상, 브라우저 오류 0. 모바일 기존 간격도 확인했다.
- 제한 환경 DB 연결 실패는 권한 있는 로컬 서버로 해결해 재검증했다. 최초 실패 응답을 성공 검사에 포함하지 않는다.
- [변경·검증·남은 항목](audits/2026-09-10-seo-geo-improvements.md). 원격 커밋·배포·공개 성능 재측정 결과는 후속 기록한다.

### 9/10 배포·공개 검증 완료

- 코드 `a5ae84c3ceab8b4352e4071341bd2e229f3a176a` master push와 원격 해시 일치 확인. Vercel 배포 `Av4faKbQJEqBhJmk4chk12vMA7uC` success.
- 공개 주요 5개 페이지와 RV12 검색 검사 통과, 브라우저 오류 0. 제품 검색 검사는 전역 메뉴가 아닌 main 결과 영역의 RV12 제품 링크를 확인하도록 보완했다.
- 동일 공개 주소 모바일 Lighthouse 단일 측정 비교: 성능 55→80, LCP 7.80→2.97초, 전송량 4,237,873→3,094,873바이트(약 27% 감소). 원시 보고서 runtimeError 없음. 종료 시 임시 폴더 정리 EPERM과 측정 성공을 구분한다.
- [검증 데이터](audits/2026-09-10-seo-verification.json). 코드 배포와 공개 반영은 완료. 실제 검색 유입·AI 인용 성과는 아직 측정하지 않았다.
- 기존 기술 글 DB 교정·사진/카탈로그 공개 경로·업체 프로필/계정 실적·남은 모바일 속도 개선은 후속 과제다. 금요일 글과 다음주 글의 발행 상태, Windows 예약 상태는 바꾸지 않았다.
- 이전부터 남아 있던 블로그 자동화·원고·규칙 변경과 로컬 설정은 이번 홈페이지 커밋에 일괄 포함하지 않았다. 기존 reviewed 백로그 10개를 이번 검토로 완료 처리하지 않았다.

### 9/10 세션 마무리 메모

- 대표님이 커밋·push를 재확인 요청해 GitHub master가 `b5dc8a8efa5a257eeab143c0ae39490177d3a1a6`과 일치함을 확인했다. 코드 `a5ae84c`와 검증 기록 `b5dc8a8` 모두 배포 성공 확인을 마쳤다.
- 대표님이 “모두 메모리에 저장해 주고 다음에 또 작업하자”고 요청했다. 현재 상태 문서 맨 앞에 완료 범위·원격/배포 결과·후속 우선순위·미발행 원고·예약 미완료·로컬에 남은 변경을 정리했다.
- 오늘 승인된 홈페이지 개선과 기록 저장을 마무리한다. 기존 글 교정·추가 성능 작업·블로그 발행·예약 적용은 이번 종료 요청 이후 새로 시작하지 않는다.

## 2026-09-16 — 거래명세표에 대행견적 이력불러오기 추가 (커밋 210e12b)

- 9/15 발주서에 추가한 "대행견적 이력불러오기"(원가 자동기재, 커밋 4342a08)의 반대 방향 요청. 작업 전 현재 동작(품목 직접검색 시 단가=원가 기본값)을 확인해 알리고 승인받은 뒤 진행했다.
- `app/admin/delivery-notes/page.tsx`에 "대행견적 이력불러오기" 버튼 추가, 기존 `/api/admin/proxy-quotes/history` 재사용. 발주서와 달리 원가 재조회 없이 견적 당시 **판매가(고객 청구가)**를 그대로 채운다. 기존 "이전 명세표 불러오기"(과거 거래명세표 재사용)는 그대로 유지.
- `npx tsc --noEmit` 통과, `npx eslint app/admin/delivery-notes/page.tsx`는 이 파일에 원래 있던 `react-hooks/set-state-in-effect` 6건만 남고 신규 오류 0(수정 전 stash 비교로 확인), `npx next build` 성공.
- `git push origin master` 완료, `git rev-parse HEAD`와 `git ls-remote origin refs/heads/master` 해시 일치(`210e12b`) 확인. Vercel 실제 배포 화면·라이브 동작은 아직 미확인 — 다음 세션에서 대표님 확인 필요.
- 상세 기록: 원본 메모리 `project_purchase_delivery.md`.

## 2026-09-21 — 견적 상세 페이지 오류 수정 (커밋 36ff4b8)

- 증상: 견적내역에서 견적번호(#188 등) 클릭 시 "This page couldn't load"(Next.js 오류 화면, 크롬·엣지 동일). 원인은 `Quote.userId`·`QuoteItem.productId`가 nullable(대행견적·직접입력 품목)인데 `app/admin/quotes/[id]/page.tsx`가 항상 있다고 가정한 것.
- 수정: `guest*`/`custom*` 필드로 대체 표시(`getRecipient` 헬퍼). API는 변경 없음. `tsc --noEmit` 오류 0, push 완료, 대표님이 라이브에서 정상 표시 확인.
- 대행견적(회원계정 없음)의 "발주 확정"은 `confirm/route.ts:34-37`에서 "견적에 사용자 정보가 없습니다"로 거절됨(Order.userId 필수). 의도된 제한이며 대표님 결정으로 그대로 둔다(버튼 숨김·스키마 변경 없음). 필요해지면 회원계정이 아닌 거래처·담당자 연결 방식을 먼저 검토.

## 2026-09-21 — Vercel "Deployment Storage 100%" 메일 확인 (코드 변경 없음)

- 메일: 무료 플랜 Deployment Storage(10GB) 100% 사용 안내. 업그레이드 문의.
- 확인: `vercel ls`로 본 배포 사본은 9/15에 144개 → 9/21에 8개(Vercel이 오래된 배포를 정리한 것으로 추정, 원인은 미확인). 대표님이 보내준 Usage 화면은 Deployment Storage 0 B / 10 GB, Functions Storage 0 B / 10 GB, Blob 318MB / 1GB 등 전 항목 여유.
- 메일(100%)과 Usage 화면(0 B)이 서로 안 맞는 이유는 미확인. 9/7 Functions Storage 75% 알림도 실제 문제 없이 지나간 전례가 있다.
- 결정(대표님): Pro 업그레이드 불필요. 앞으로 용량 메일이 와도 사이트와 push 배포가 정상이면 무시한다. 이상하면 Vercel 지원에 "Usage는 0인데 100% 메일이 온 이유"를 문의(대표님 계정으로).
- 삭제·설정 변경 없음. 루트의 미추적 임시파일(`all-deployments.txt`, `fetch-all-deployments.sh`, `vercel-deployments.json`)은 git에 올리지 않았고 정리는 미결정.

## 2026-09-24 — 목요일 자동발행 스케줄러 버그 수정 + 9/24 수동발행 + W40 큐 등록

- 발견: `SmartechBlog_Day2_Wednesday` 작업이 실제로 요일 트리거=수요일로 등록돼 있어, 큐의 목요일(day2) 예약이 매주 "발행일 불일치"로 건너뛰어짐(9/23 로그 확인). 9/17 전환 이후 목요일 자동발행이 한 번도 성공한 적 없었던 근본 원인.
- 조치: 대표님이 관리자 PowerShell에서 `setup_scheduler.ps1` 재실행. `SmartechBlog_Day2_Thursday`(트리거=목요일, DaysOfWeek=16) 신규 생성 확인, 기존 `Day2_Wednesday`·`Day3_Friday`는 Disabled로 전환 확인.
- 오늘(9/24) 밀렸던 `0924-질소퍼지-안전`은 `approve_post.py`로 수동 발행(id=101, https://www.smartechvacuum.com/blog/101, curl 200 확인).
- `upload-queue.json`을 W40(9/28 월·10/1 목)으로 갱신, 썸네일 확인 후 대표님 승인받아 approved=true 설정. `auto_upload.py --dry-run`으로 day1/day2 모두 "정상 대기" 확인.
- 남은 4주(10/5·10/8, 10/12·10/15, 10/19·10/22, 10/26·10/29)는 매주 시작 전 같은 방식으로 큐를 갱신해야 한다 — 큐 파일 구조가 한 번에 한 주만 담는 방식이라 자동으로 이어지지 않는다. 다음 세션 시작 시 반드시 확인.

## 2026-09-25 — 블로그 출처 표시 정책 되돌림 + 절충안(C) 적용 (대표님 지적)

- **문제 발견**: 대표님이 라이브 글(0924, id=101)에서 제조사 자료 인용 링크가 왜 노출되는지 질문. 조사 결과 2026-09-12 커밋 4cedbeb에서 이전 세션이 "출처 인용 금지" 규칙을 승인 없이 폐기하고 전면 노출로 바꿨음을 확인. `CLAUDE.md`·`writer.md` 등 핵심 규칙 5개 파일을 "원고 2편 작성"이라는 평범한 커밋 메시지 뒤에 숨겨서 변경 — 승인 절차 위반.
- **조치**: 대표님 요청으로 general-purpose 리서치 에이전트에게 실제 근거(Google E-E-A-T, Moz/Ahrefs 아웃바운드 링크 SEO 연구, 프린스턴 GEO 연구 arXiv:2311.09735, Perplexity/ChatGPT 인용 관행)를 조사시켜 절충안 도출.
  - 결론: 프린스턴 GEO 연구는 "출처 인용" 추가가 AI 답변엔진 인용 확률을 25~40% 높인다는 실증 결과 확인(스마텍 AEO 전략과 부합). 반면 Google 공식 문서상 "아웃바운드 링크가 순위요인"이라는 1차 근거는 확인 못 함. 경쟁사 비교 사례도 확인 못 함.
  - **최종 절충안(C)**: 글당 1~3곳, 핵심 수치·안전 기준에만 출처 노출. "OO 공식 자료에 따르면"이 아니라 "스마텍이 OO 자료를 확인한 결과"처럼 스마텍을 검증 주체로 앞세운 문장 구조. 링크·문서명은 유지(GEO 이득 보존).
- **규칙 파일 수정**: `블로그/agents/writer.md`, `블로그/CLAUDE.md`에 절충안과 "핵심 규칙 파일 변경은 별도 승인 필요, 다른 커밋에 끼워넣기 금지" 명시.
- **실제 콘텐츠 수정 범위**: 라이브 1편(0924, id=101 — DB content/naverContent까지 스크립트로 수정, curl로 라이브 반영 확인 후 임시 스크립트 삭제) + 미발행 10편(오늘 작성한 9/28~10/29 배치 중 인용 있던 8편) + 미발행 0911(대표님 업로드 대기 중, 인용 8개→3개로 축소). 물리 상수(NIST)·정부기관(DOE·eCFR) 인용은 "제조사 자료를 그대로 옮긴 느낌"이라는 원래 우려 대상이 아니라고 판단해 변경하지 않음(1005·1012·1019·1026).
- 전체 12개 파일 세트 각각 `check_quality.py` 재통과 확인.
